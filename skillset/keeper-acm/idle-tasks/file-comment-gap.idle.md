Pick one file in the `acm-*` legacy tree (a Java class, or a `.tpl` template) that's been read and actually understood but lacks a good comment/header. Add a short comment explaining *why this file is distinct from all the others* — its specific reason for existing, the non-obvious thing about it (e.g. which skin lineage it belongs to, why it diverges from its prototype) — not how it works line-by-line and not a restatement of its interface. One file per day, cumulative; don't re-touch a file already logged in `processed/`.
