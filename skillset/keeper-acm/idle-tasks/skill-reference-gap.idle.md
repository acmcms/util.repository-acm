While reading, if you hit a template-language construct, a `skin.settings.xml` field, or a skin lineage that isn't yet captured in this skill's own reference sections in `SKILL.md` (the template-language tag list, the `skin.settings.xml` field list, or "Known skin lineages"), add a short, grounded entry for it there instead of just leaving a note. Example of exactly this gap, found but not yet acted on: the `ACM.ECMA` renderer and its `<%CODE: 'ACM.ECMA' %>` tag, seen in `acm-com.vlapan/vlapan-richedit/skin.settings.xml` but absent from the template-language notes.

Don't guess at the general shape from a single example — only add what you've actually confirmed in the source, and say so briefly. Log the addition like any other idle-day entry, as a new dated file in `processed/`.
