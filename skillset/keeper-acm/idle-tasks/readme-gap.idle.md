Look for a package/directory in the `acm-*` tree (or an AE3 skin package it depends on) that has no README (or a near-empty placeholder) and would benefit from one — same "read and actually understood" standard as `file-comment-gap.md`: don't write it until you can say something true and specific about the directory. Write a short README covering what the package/directory is and why it's distinct from its siblings (e.g. which skin lineage it belongs to, which repo boundary it sits on) — not a full manual, not exhaustive API docs.

This is specifically about READMEs. A missing CLAUDE.md next to an *existing* README is a separate, already-covered convention — filled in gradually and incidentally as a side-effect of other work, never as a dedicated sweep — don't conflate the two or use this task to backfill CLAUDE.md instead.

One directory per day, cumulative; don't re-touch one already logged in `processed/`.
