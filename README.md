# util.repository-acm

Bootstraps an `acm` workspace from scratch — clones/updates the set of repos an `acm` workspace needs. Run the install one-liner in `sh-scripts/install-acm-repository.sh`, or `local-checkout-acm-from-git.sh.txt` for a local-checkout variant.
