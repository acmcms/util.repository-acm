# CLAUDE.md — util.repository-acm

The repo list this tooling clones/updates lives in `sh-data/repository/remotes-list-acm.txt` — the authoritative `<local-path> <git-remote-url>` pairs for a given workspace. Only a subset of the full set of `acm`-namespace repos may be listed here for any one workspace — check this file before assuming an `acm` repo doesn't exist anywhere.
